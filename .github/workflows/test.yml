name: test
on: 
  push: 
    branches:
      - 'develop'
  
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      
      - name: hello
        if: ${{ github.ref_name == 'develop' }}
        run: |
          echo "goodbye"
          echo "line 2"
      